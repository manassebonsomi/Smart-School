import vine from '@vinejs/vine'
export const indexAdministrateurValidator = vine.compile(vine.object({page: vine.number().min(1).optional(),limit: vine.number().min(1).max(100).optional(),search: vine.string().trim().optional(),statut: vine.enum(['ACTIF','INACTIF','BLOQUE','EN ATTENTE','SUPPRIME']).optional(),sortBy: vine.enum(['nom','prenom','email','created_at']).optional(),order: vine.enum(['asc','desc']).optional()}))
